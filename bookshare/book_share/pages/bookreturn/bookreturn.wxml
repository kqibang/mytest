<!--pages/bookreturn/bookreturn.wxml-->
<view class="container1" wx:for="{{borrows}}" wx:key="id">
    <view class="container3">
        <view class="text">
            书名：{{item.b_name}}
        </view>
        <view>
            作者：{{item.b_author}}
        </view>
        <button bindtap="returnTap" type="primary" class="btu" data-brid="{{item.br_id}}">归还</button>
    </view>
</view>

